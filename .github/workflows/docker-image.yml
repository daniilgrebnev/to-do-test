name: Docker Compose

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      docker:
        image: docker:20.10.8
        ports:
          - 8080:8080
          - 3000:3000
          - 7432:5432 # Укажите порты, которые нужны для службы
        options: --entrypoint ""

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Compose
        run: |
          sudo docker-compose -f docker-compose.yml build
          sudo docker-compose -f docker-compose.yml up -d

      - name: Wait for services to start
        run: sleep 30 # Подождите, пока службы запустятся

      - name: Run tests or other actions
        run: |
          # Здесь вы можете выполнить свои тесты или другие действия

      - name: Tear down services
        run: sudo docker-compose -f docker-compose.yml down
